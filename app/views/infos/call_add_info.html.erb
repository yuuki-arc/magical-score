<% @infos.each do |info| %>
  <table class="table table-striped table-hover table-condensed">
    <thead>
      <tr>
        <th colspan="5"><%= info[:title] %></th>
      </tr>
    </thead>
    <tbody>
      <% info[:mode].each do |mode| %>
        <tr class="<%= get_difficulty_text(mode) %>">
          <td style="width:14%;"><span class="label <%= get_difficulty_label(mode) %>"><%= info[mode][:star] %></span></td>
          <td style="width:11%;"><img src="<%= info[mode][:detail][:image] %>" width="25" height="26" border="0"></td>
          <td style="width:24%;"><%= info[mode][:detail][:rate] %></td>
          <td style="width:21%;"><%= info[mode][:detail][:point] %></td>
          <td style="width:30%;"><%= info[mode][:detail][:trial] %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>
